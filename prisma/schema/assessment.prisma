model Assessment {
  id          String       @id @default(cuid())
  title       String
  maxMarks    Int
  weightage   Int
  moduleId    String
  submissions Submission[]
  deadline    DateTime
  module      Module       @relation(fields: [moduleId], references: [id])
  grades      Grade[]
}
